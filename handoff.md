# Handoff
## Completed: Official transition v6 + two independent coop-route-25 campaigns passed; campaign per-window pixel contract fixed
## Next Task: One more independent route campaign, then curated combat/drop, save/reload, and 60-minute soak gates
## Context: M5 remains In progress. `coop-transition-west` v6 passed. The first physical-keyboard `coop-route-25` run passed 25/25 transitions in 373 seconds with verified cleanup. The second independent `coop-route-25` campaign, artifact `campaign-20260820T193156808Z-9be43d949b066edc`, passed 25/25 in 330 seconds on parent build `0.2.0+dcb7006c795d9e824937381e108a2e7e7db4b1f4` with co-op `0.4.0`; a generation-checked diagnostic reset cleared the initial save-load transient state and the session finished at diagnostic generation 1. Cleanup was attempted, succeeded, and verified. The campaign's remaining defect was in the parent observer: `postTransitionCommandedPixels` resets at each completion, so a baseline delta can go negative before P2 moves; commit `91bd89b` now requires current pixels >=8 and includes a regression test. Parent tests 164/164 and MCP Release build are clean. Co-op commits `8c91458` (identity-based room accounting) and `286fbda` (settled bounds before reconstruction) are live-verified; `ce961a9`'s player-load initial-layer behavior has focused regression coverage but remains pending live verification. Route legs have exact +1 completion/+1 pass and zero failure deltas. At llvmpipe's roughly 17fps, keyboard holds need scaled wall time (west Left 3s + Z 0.3s + Left 3s; east Right 5s; P2 inward 2s).
## Files Modified: handoff.md; parent contract fix is `91bd89b`
