# Handoff
## Completed: Live route campaign investigation; co-op transition-accounting defect found
## Next Task: Co-op reducer transition-episode dedupe fix, then transition-west v5 and coop-route-25 live runs; remaining combat/drop/save/soak gates
## Context: M5 In progress. The route campaign observer itself works (preflight, room-sequence checks, cleanup verified on all failures), but six attempts failed on per-transition metric contracts because the co-op mod counts multiple transition completions per physical door crossing (+1 only with frame-exact automation timing, +2/+3 with ordinary keyboard input, varying with session state). Fix belongs in the co-op movement-session reducer (dedupe transition episodes per physical crossing). Pad 2 keyboard via host Keys2 bindings + XTest drives non-automation input fine. Patched memory card spawns Marble Gallery room 140. Route v2 (140/220 cyclic, fingerprint b1f6e989...), scenario v5 (hop timeline), and area-40 room identity corrections are committed. Parent 149/149, co-op gate green.
## Files Modified: handoff only this commit; prior commits carry all route/scenario/catalog corrections
