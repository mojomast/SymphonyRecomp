# Handoff
## Completed: Official transition v6 + first coop-route-25 campaign passed; campaign per-window pixel contract fixed
## Next Task: Two more independent route campaigns, then curated combat/drop, save/reload, and 60-minute soak gates
## Context: M5 remains In progress. `coop-transition-west` v6 passed. The first physical-keyboard `coop-route-25` run passed 25/25 transitions in 373 seconds with verified cleanup. The campaign's remaining defect was in the parent observer: `postTransitionCommandedPixels` resets at each completion, so a baseline delta can go negative before P2 moves; commit `91bd89b` now requires current pixels >=8 and includes a regression test. Parent tests 150/150 and MCP Release build are clean. Co-op commits `8c91458` (identity-based room accounting) and `286fbda` (settled bounds before reconstruction) are live-verified; route legs have exact +1 completion/+1 pass and zero failure deltas. At llvmpipe's roughly 17fps, keyboard holds need scaled wall time (west Left 3s + Z 0.3s + Left 3s; east Right 5s; P2 inward 2s).
## Files Modified: handoff.md; parent contract fix is `91bd89b`
